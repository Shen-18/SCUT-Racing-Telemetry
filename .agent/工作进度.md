
# 工作进度

本文件是当前工作状态与工作日志的唯一入口；实施要求以施工手册为准，原始验收证据存放在 `.agent/evidence/`。记录日期使用 Asia/Shanghai 时区。

## 当前状态

- 当前阶段：Step 1–5 验收完成；**UI 设计定稿（D16 · F1 官方风格）**——附录 B 重写为 rev.5、新增 B.11 交互约定、DESIGN-SPEC 第 9 节落地决策、ADR-0008；下一步按 **Step 5R**（R1 令牌 → R2 壳层 → R3 中栏 → R4 右栏 → R5 播放）重构现存界面。
- 设计基准：`design-demos/08-f1-data-analysis.html`（负责人已审核）+ `design-demos/DESIGN-SPEC.md`。关键决策：固定三栏可拖调宽（dockview 移除）、双主题红+黑/红+白、状态栏保留、圈速面板取消、游标改按住拖动模型。
- 未完成 / 边界：Step 5R 未实施；SIZE chip 需后端补文件大小字段；Formula1 Display 字体版权边界见 ADR-0008（仅内部使用）。

## 工作日志（按时间追加）

### 2026-09-11 · 01 · Step 1 动态复验（补录）

- 目标：在用户修复 PATH、PowerShell 5.1 兼容性并纳入前端门禁后重新验收。
- 实际工作：主 agent 委派 agy-staff review 执行并读取日志核对；本轮未修改源码、配置或手册。图标已在此前补齐，本轮完成 SHA256 对比，确认与旧工程源图标一致。
- 执行者：agy-staff review；job `review-mtwt7rkz-36694f44`。
- 命令：在项目根运行 `powershell -NoProfile -File tests/tooling/check.ps1`。
- 结果：真实进程退出码 0；依赖契约 PASS、Python 回归 4 项通过；rustfmt、clippy、cargo workspace test、TypeScript、ESLint、Vitest、Vite build、golden suite 八道门禁 PASS。
- 测试边界：Vitest 1 项、Rust golden 1 项；golden 同时包含在工作区测试与独立关卡中，去重后共 6 项自动化用例。其余 Rust 基础壳尚无单元用例；golden 仅验证既有 SQLite fixture 身份，不代表遥测数值正确性。
- 证据：[完整日志](evidence/step1/reacceptance-20260911-check.log)、[审查报告](evidence/step1/reacceptance-20260911-review.md)。
- 未完成：GUI 未测；旧成功文案及依赖检查覆盖问题尚未修复。此前 SDK 链接阻塞已不在本轮复现，不再作为当前阻塞。

### 2026-09-11 · 02 · 建立持续进度记录

- 目标：按用户要求建立固定进度文档，并规定每次工作结束都记录。
- 实际工作：新建本文件；施工手册增加进度维护约束；文档入口和目录说明增加指针；旧 Step 1 状态文件加历史标记，保留原始记录。目录说明中的验收命令同步为 Windows `powershell`。
- 涉及文件：`.agent/工作进度.md`、`.agent/Workbuddy_20260910_0008_Tauri_v2_Construction_Manual.markdown`、`.agent/README.md`、`.agent/工作区目录说明.md`、`.agent/evidence/step1/STATUS.md`。
- 验证：仅文档编辑与静态回读，未重新运行测试，未修改产品代码；沿用上一条验收证据，不冒充本次新跑结果。
- 下一步：继续按施工手册处理 Step 1 剩余验收与审查事项；本次记录不授权自动进入后续步骤。

### 2026-09-11 · 03 · Step 2 核心数据模型与算法

- 目标：按施工手册完成 `telemetry-core` 的模型、纯算法、金字塔与测试。
- 实际工作：建立 `models.rs`、`downsample.rs`、`pyramid.rs`、`gps.rs`、`stats.rs`、`align.rs`；补充 frozen core contract；增加核心与金字塔集成测试、doc tests；修正 specta derive 依赖、窗口截断端点、NaN/未配对尾部、WGS84 极点边界与格式。
- TDD 证据：agy-staff 首轮红灯 exit 1，先前编译/行为测试暴露 9 项失败，见 `evidence/step2/red.log` 与 `red-all.log`；修复后核心测试 19 项通过。
- 执行者：主 agent 编写主实现；agy-staff implement job `implement-mtwupwd4-ce351959` 与 `implement-mtwv535x-66138a16` 执行格式化/测试。
- 验证：`powershell -NoProfile -File tests/tooling/step2-check.ps1 -Phase final-core` exit 0；`cargo test -p telemetry-core --all-features` exit 0，19 passed；全局 `tests/tooling/check.ps1` exit 0。证据 `evidence/step2/final-core.log`。
- 审查：agy-staff review job `review-mtwuwhwp-624d24c0` 动态核心与全局门禁 exit 0，但先发现 2 个边界问题；已由后续修复处理。其报告保留于 `review-round2.md`，最终修复尚需独立终审确认。
- 当前边界：Step 2 核心代码和自动化测试已完成；`estimate_offset` 按手册延至 Step 15。Step 1 GUI 验收仍未完成；未提交 Git。
- 下一步：使用 agy-staff 对最终 diff 做一次只读终审；根据终审结果决定是否将 Step 2 标为通过。

### 2026-09-11 · 04 · Step 2 独立终审与记录纠正

- 目标：按用户“做审查”要求，对最终修复状态进行独立只读终审与动态验收。
- 执行者：agy-staff review；job `review-mtwvesww-848275dd`，模型 `gemini-3.8-flash-high`。主 agent 读取最终报告、日志及 stats.rs，核对结果并维护本进度文档。
- 验证：下列六条命令各自退出码均为 0：
  1. `powershell -NoProfile -File tests/tooling/step2-check.ps1 -Phase final-review`
  2. `cargo test -p telemetry-core --all-features`
  3. `cargo test -p telemetry-core --doc`
  4. `cargo clippy -p telemetry-core --all-targets --all-features -- -D warnings`
  5. `cargo fmt -p telemetry-core -- --check`
  6. `powershell -NoProfile -File tests/tooling/check.ps1`
- 结论：终审 PASS，无阻塞发现；核心 16 项集成测试及 3 项 doc tests 通过，合计 19 项，重复执行的 doc tests 不另加计数。全局依赖检查及八道门禁通过，报告中的“六道”沿用了脚本旧成功文案，不是实际门禁数量。
- 非阻塞发现 F-01：rmse_and_corr 未像 downsample/pyramid 一样拒绝非有限窗口；NaN 边界可能被 max/min 吸收并按单侧边界计算。现有契约未规定此行为，后续统一校验并补测试；本轮没有修复，避免改变刚验收的代码状态。
- 证据：[独立终审报告](evidence/step2/final-review.md)、[完整动态日志](evidence/step2/final-review.log)。原始报告保持不变；其中“11 unit tests”实际为核心集成测试，19 项已包含 3 项 doc tests。
- 第 03 条记录纠正：最初实现先于测试，不能宣称严格遵循 TDD；之后才由 agy-staff 执行红灯回归与修复后验证。`implement-mtwupwd4-ce351959` 不仅格式化/测试，还修复了极点高程与文档；`implement-mtwv535x-66138a16` 修改了 stats.rs 并增加回归测试。此前对 implement 作业的归因不完整，违反“主代码由主 agent 自写”的分工，应如实保留并在后续恢复该分工。
- 未完成 / 下一步：Step 1 GUI 验收仍未完成；本轮到终审结果为止，未进入 Step 3、未提交 Git。用户授权推进后再按手册开始下一步。

## 追加模板

### YYYY-MM-DD · 当日序号 · 工作标题

- 目标 / 所属 Step：
- 实际工作 / 涉及文件：
- 验证：执行者、命令、真实退出码、结果；未运行时写明原因。
- 证据：日志 / 报告链接；使用 agy-staff 时记录 job id。
- 未完成 / 阻塞 / 下一步：

### 2026-09-11 · Step 3 继续整改

- 目标：补齐 aim-ffi 的真实 DLL 桥并由 agy-staff 动态审查。
- 实际工作：修正 GPS 插值通道时间戳从毫秒转秒、速度 m/s 转 km/h；补充 SessionMeta.sample_rate_hz；让 open_xrk_laps 走 lap 族导出函数；补充 Du 与 lap 真实 DLL 测试；修正重复通道键格式。
- 验证：主 agent 执行 cargo fmt；cargo clippy -p aim-ffi -- -D warnings，退出码 0。此前真实 AGX 与错误路径测试退出码 0；新增测试尚未由 agy-staff 完成全量验收。
- 审查：agy-staff review job review-mtwynx6q-473f052c，动态审查进行中；上一轮 review-mtwy08u5-22bc2d71 因 prompt 转义目标丢失而判定无效。此前有效报告 review-mtwy9vsz-2a49791e 明确 FAIL，指出 raw GPS 派生、golden、元数据、测试覆盖和单文件长度等问题。
- 未完成：GPS raw ECEF 派生、完整元数据 date/time、golden fixture、50 次测试自动化、unsafe 审计与模块拆分仍待处理；不能将 Step 3 标记为 PASS。

### 2026-09-11 · Step 3 动态整改续记

- agy-staff review `review-mtwzxazq-4d33191e` 已完成，结论 FAIL；报告确认 GPS raw 派生已接入但被官方通道同名遮蔽、Distance 选源不确定、测试覆盖不足等问题。
- 主 agent 修复：官方插值 GPS 改为 `(AiM Interpolated)` 诊断名；Distance 明确选择 `DerivedGps` GPS Speed；增加 Du、lap selection、50 次循环测试，并以测试锁串行化 DLL 访问，避免 vendor 临时 mmap 文件并发冲突。
- fresh verification：`cargo fmt -p aim-ffi`、`cargo clippy -p aim-ffi -- -D warnings`、`cargo test -p aim-ffi --test step3` 均退出 0；Step 3 专项测试 5 passed。
- 未完成：仍需重新 agy-staff review；golden fixture、完整 metadata、unsafe 审计/模块拆分、损坏文件/中文路径正式断言、45 export inventory 仍需继续处理。

### 2026-09-11 · Step 3 继续实现与验证（本轮）

- 目标：继续补齐 `aim-ffi` 的真实 DLL 桥、45 个导出绑定、元数据和 Step 3 测试资产。
- 实际工作：
  - 修复 `open_xrk_laps` 的 BTreeMap 合并键类型，按 `(name, unit, source discriminant)` 合并并保持 source；
  - `AimDll` 绑定 `MatLabXRK.h` 声明的 45 个导出（含 licence/name-close、device、no-spaces、GPS 频率及库诊断导出），仍只在 actor 线程调用；
  - 从 `get_date_and_time` 填充 `SessionMeta.date` 与 `SessionMeta.start_time`；标准化通道名/单位空白，官方 GPS 插值通道速度按 m/s→km/h，时间戳按毫秒→秒；
  - 增加真实损坏文件、中文/空格路径、golden fixture 和元数据测试；生成 `tests/golden/agx_speed.bin`、`tests/golden/agx_csv_meta.json`。
- 验证：`cargo fmt -p aim-ffi` 通过；`cargo clippy -p aim-ffi -- -D warnings` 退出码 0；Step 3 非-golden 7 项真实 DLL 测试退出码 0（含 Du、lap、50 次打开/关闭、损坏文件、中文/空格路径）。
- 当前失败：golden 测试发现 AiM DLL 官方 GPS 插值序列与 `AGX.csv` 在相同时间点存在超过 `1e-4` 的真实差异（首个失败点约 `t=0.900`，actual `0.12160323`、CSV `0.1197`），因此未伪造 PASS；需决定采用官方导出作为 golden 基准、修正 CSV/DLL 对齐算法，或保留该数据源差异为阻塞。
- 其他：运行 50 次测试产生大量 vendor 临时 `AGX-*` 目录；未删除用户 Temp 数据。未提交 Git。

### 2026-09-11 · Step 3 临时目录清理与概念说明

- 说明：`GPS Speed (AiM Interpolated)` 是 AiM 官方 DLL 通过 `get_GPS_channel_samples` 导出的 GPS 通道；`GPS Speed`（`DerivedGps`）是 Rust 从 `get_GPS_raw_channel_samples` 提供的 ECEF 原始位置/速度分量自行派生的速度。前者不是本项目新增算法，后者才是本项目的 raw GPS 派生结果。
- 清理：按用户要求删除本轮测试生成的 `C:\Users\Shen\AppData\Local\Temp\AGX-20260911_*` 临时目录，共 748 个；删除前已限定目标父目录和名称前缀。

### 2026-09-11 · Step 3 官方输出规则切换、复核与收尾

- 目标：按用户要求将 Step 3 数值真值切换为 AiM 官方 DLL 输出，修改验收规则，并完成一次复核。
- 实际工作：
  - `GPS Speed (AiM Interpolated)` 明确作为官方 DLL golden；`GPS Speed` 保留为 raw ECEF 派生值，`Distance on GPS Speed` 继续使用 raw-derived speed。
  - golden 测试改为完整序列比较：样本数、每个时间戳、每个值均校验；解析 `agx_csv_meta.json`，同时验证 sample count、source、SHA-256。
  - 基础 AGX/Du/lap 测试补充官方 GPS、DerivedGps、laps、时间单调性断言；多 lap 合并后重新按时间排序。
  - 施工手册已明确 CSV fixture 只用于 CSV 元数据，不作为 DLL 数值真值。
- 验证：`cargo fmt --all -- --check` 通过；`cargo clippy --locked -p aim-ffi --all-targets -- -D warnings` 通过；`cargo test -p aim-ffi --test step3 -- --nocapture` 退出码 0，8 passed。
- 复核结论：官方输出规则和 golden 覆盖已符合本轮目标。保留的后续改进项：50 次循环当前验证资源可重复打开关闭，但未做独立 Windows 句柄基线采样；`SetDllDirectoryW` 的进程级副作用与 FFI RAII/边界审计仍可继续加强。它们不影响本轮官方输出规则切换。
- 清理：已在最终验证后清理 `%LOCALAPPDATA%\Temp\AGX-20260911_*` 及本测试生成的 `aim-ffi-step3-*` 临时目录；未提交 Git。

### 2026-09-11 · Step 4 开始：telemetry-ipc 协议基础

- 自审结论：Step 3 的剩余项属于测试强度、进程级 DLL 环境副作用和 FFI 防御性审计，不影响官方输出规则、数据来源约定或进入 Step 4；按用户决定不继续死磕。
- 进入条件：Step 3 官方 DLL golden、核心通道来源规则、错误路径和基础验证均已通过；允许继续实现 IPC/Tauri 薄壳。
- 本轮实现：在 `crates/telemetry-ipc` 冻结 Request 全变体、ImportStage、ImportStatus、CacheRootStatus、CmdError；增加 JSON serde 往返；实现 `SXK1` little-endian 二进制帧编解码及长度校验。
- 验证：`cargo test -p telemetry-ipc` 通过，3 个单元测试和 doc tests 通过；已执行 `cargo fmt --all`。
- 下一步：继续实现 `src-tauri` 的 Job 状态机、命令路由和导入/打开闭环，再按 Step 4 规则补齐 specta/TypeScript 同步与状态迁移测试。

### 2026-09-11 · Step 4 独立审查（未通过）

- 审查范围：对照施工手册 Step 4 rev.4，读取 `telemetry-ipc`、`src-tauri`、`src/api/client.ts`、前端测试及 Tauri 配置，并执行 workspace 测试。
- 关键 findings：Step 4 仅完成协议基础，尚未完成交付闭环。`src-tauri/src/main.rs` 仍是空 Tauri builder，无 `AppState/jobs`、命令路由、异步导入 Job、状态轮询、取消/优先级、OpenDataset/WindowSeries；前端 `client.ts` 仍为空，未实现协议命令；不存在 specta→TypeScript 生成/同步门禁、DatasetMeta/WindowFrame 类型和 generation 丢弃逻辑；`tauri.conf.json` 未配置 `Data/SCUTRacing.ico`、NSIS/Webview2 embedBootstrapper，且 bundle inactive；缺少导入 AGX→Ready→OpenDataset→WindowSeries 的集成验收与点击优先实测。
- 本轮修复：删除 `telemetry-ipc` 对 `cache-core`/`telemetry-store` 的错误反向依赖，消除 workspace 循环依赖；清理 `cache-core` 未使用导入。该修复仅恢复 workspace 构建健康，不代表 Step 4 完成。
- 验证：`cargo generate-lockfile --offline`；`cargo test --workspace` 通过（Step 4 协议 4 tests，workspace 其余测试及 doc tests 通过）。此前 `cargo test --locked --workspace` 曾因上述循环依赖/陈旧 lock 失败，修复后需重新锁文件后再执行 locked 验收。
- 结论：**Step 4 NOT PASS / 阻塞**。不能将当前协议基础冒充为手册要求的 Tauri 薄壳与导入-打开-出图闭环。

### 2026-09-11 · Step 4 实现轮次

- 已补齐 cache-core DatasetCache/read_window_frame、Tauri AppState/jobs、StartImport/ImportStatus/CancelImport 薄壳及 NSIS/WebView2 配置。
- 验证：telemetry-ipc/cache-core/src-tauri 定向测试通过；workspace clippy -D warnings 通过。
- 独立审查仍在进行；在其 PASS 前不宣称 Step 4 完成。

### 2026-09-11 · Step 5 GUI 切片推进

- 根据 Step 4 审查结果先推进可视化壳层：完成深色 telemetry workspace、导入路径输入、状态灯、通道侧栏、绘图区占位及状态栏；补充 typed Tauri IPC client，并添加 @tauri-apps/api 依赖。
- 前端验证：pnpm typecheck、pnpm test -- --run、pnpm build 均通过；证据 .agent/evidence/step5/gui-slice.md。
- 仍未宣称 Step 4 完全通过：Rust 端完整导入闭环和真实数据出图仍需后续接线。

### 2026-09-12 · Step 4 修复复审（NOT PASS）

- 验证：`cargo fmt --all -- --check`、`cargo clippy --locked --workspace --all-targets -- -D warnings`、`cargo test --locked --workspace`、`pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm build` 全部退出 0。
- 阻塞 findings：`src-tauri/src/main.rs` 的 `start_import` 立即伪造 Ready，未读取 AGX/XRK；`window_series` 返回 `sin()` 合成数据，未读取 `cache_core::DatasetCache::read_window_frame`；前端 `App.tsx` 仍只有导入占位，未轮询 ImportStatus、OpenDataset 或绘图；client 暴露了 `cursor_values`、`laps`、`stats`、`list_records`、`export_csv`、`purge_cache` 等未注册命令，调用必失败；协议要求的 DatasetMeta/窗口帧闭环和专项集成测试仍不存在。
- 结论：允许性能与真实 AGX 数值暂缺，但不能接受合成数据、未注册命令和未完成导入-打开-出图闭环。因此 Step 4 **NOT PASS**。

### 2026-09-14 · Step 4 完整验收通过

- 修复真实闭环：AiM actor metadata/read_channels、SHA-256 metadata-first cache、raw/pyramid 持久化、优先队列/取消、Tauri lazy OpenDataset、预计算 WindowSeries SXK1 generation 回显、前端 250ms 轮询/解码/过期帧丢弃。移除旧的 Ready 伪造与 sin 合成数据。
- 验收证据：.agent/evidence/step4/final-acceptance-20260914.md。Rust workspace 全测试、clippy、fmt、IPC 23 变体/帧/metadata、cache persistence、真实 AGX 端到端、前端 typecheck/lint/20 tests/build 均通过。
- gy-staff review 两次因本机 spawn EPERM 无法启动；未将其冒充 PASS，改以逐条本地审查和真实测试证据完成验收。性能指标未作为阻塞。

### 2026-09-14 · Step 4 复核收口

- 补充 `telemetry-ipc/tests/specta_sync.rs`：真实注册 Rust Specta 类型集合，并校验提交的 TypeScript 合同声明与关键字段；`tests/tooling/step4-check.ps1` 已纳入该测试。
- 重新执行：`cargo fmt --all -- --check`、`cargo clippy --locked --workspace --all-targets -- -D warnings`、`cargo test --locked --workspace`、`tests/tooling/step4-check.ps1`、`pnpm typecheck`、`pnpm lint`、`pnpm test -- --run`（20 tests）、`pnpm build`，全部 PASS。
- 独立逐项复核记录：`.agent/evidence/step4/independent-review-20260914.md`；无剩余功能性阻塞。agy-staff 因本机 `spawn EPERM` 未能启动，未冒充外部 PASS；性能数值不作为阻塞条件。

### 2026-09-14 · Step 5 前端骨架与垂直切片交付验收

- 目标：完成施工手册 Step 5：前端骨架与垂直切片交付验收。
- 实际工作：
  - 完善依赖契约：在根 `Cargo.toml` 注册 `serde_json` 与 `sha2` workspace 依赖，各 crate 改为 `.workspace = true`，并在 `config/contracts/dependencies.json` 白名单化 `serde_json`、`sha2` 与 `@tauri-apps/api: 2.0.0`，修复 `verify_dependencies.py` 门禁；
  - 后端注册全部 23 条命令：在 `src-tauri/src/main.rs` 将 `stats`、`list_records`、`delete_record`、`export_csv`、`comments`、`add_comment`、`delete_comment`、`save_layout`、`load_layout`、`estimate_offset`、`purge_cache` 等补全至 `tauri::generate_handler!`；
  - 前端类型统一：在 `src/api/client.ts` 复用 `src/api/types.ts` 导出的 `ImportStage`、`ImportStatus`、`FrameHeader`、`WindowFrame`，并统一 `ChannelMeta` 扩展字段定义；
  - 设计令牌完整引入：在 `src/main.tsx` 与 `src/index.css` 导入 `src/theme/tokens.css`，确保生产 CSS bundle 包含设计令牌 CSS 变量；
  - 交互式 `TimelineBar`：实现鼠标拖拽与边界夹取算法 `calculateTimelinePan`，视口超出两侧自动 clamp，短视口拖拽不漂移，并通过 `TimelineBar.test.tsx` 单元测试；
  - 非终态导入优先级联动：在 `src/state/appStore.ts` 实现 `isNonTerminalImportStage` 与 `toggleChannelAndPrioritize`，在 `ChannelTree` 切换通道勾选时自动发起 `prioritizeImport`，并通过 `ChannelTree.test.ts` 单元测试；
  - 垂直切片端到端集成测试：在 `src/vertical-slice.test.ts` 覆盖 AGX 导入、`meta_ready` 通道显示、Ready Speed WindowSeries 渲染、缩放/平移 generation 递增及过期帧丢弃完整垂直切片，无任何合成遥测数据。
- 验证：
  - `pnpm typecheck` 退出码 0；
  - `pnpm lint` 退出码 0（0 warnings）；
  - `pnpm test -- --run` 退出码 0（10 个测试文件，68 项测试全部通过）；
  - `pnpm build` 退出码 0（构建正常，含设计令牌 CSS）；
  - `cargo fmt --all -- --check` 退出码 0；
  - `cargo clippy --locked --workspace --all-targets -- -D warnings` 退出码 0；
  - `cargo test --locked --workspace` 退出码 0；
  - `powershell -NoProfile -File tests/tooling/check.ps1` 退出码 0（全八道门禁全部通过）。
- 证据：`src/vertical-slice.test.ts`、`src/components/TimelineBar.test.tsx`、`src/panels/ChannelTree.test.ts` 及门禁全通过。
- 未完成 / 边界：按指令要求未执行 git commit / push。

### 2026-09-14 · Step 5 复审阻塞项整改（默认速度通道与点击优先实测）

- 目标：解决 Step 5 复审发现的剩余真实阻塞：1) 消除硬编码 `key="Speed"`，按优先级选择真实 AGX 速度通道；2) 针对实际后端/IPC 运行 `prioritize_import`，建立可重复的真实落盘证据。
- 实际工作：
  - TDD 先行失败测试：
    - 新建 `src/api/channelSelection.test.ts`（7 项测试），测试官方 `GPS Speed (AiM Interpolated)`、raw `GPS Speed`、标准 `Speed`、大小写不敏感 `speed`、过滤 `Distance on GPS Speed` 及无速度通道回退首通道逻辑；首轮运行 `pnpm test` 确认为红灯失败（exit 1）；
    - 在 `src/state/appStore.test.ts` 补充针对真实 AGX 元数据的 `openDataset` 默认通道选择失败断言；首轮运行确认为红灯失败（exit 1，收到 `Battery_V` 而非硬编码 `Speed`）。
  - 实现真实速度通道优先选择：
    - 在 `src/api/client.ts` 实现并导出 `selectDefaultSpeedChannel`，优先匹配官方 `GPS Speed (AiM Interpolated)` 或 source 为 Gps/Standard/AiM 的速度通道，其次匹配 raw `GPS Speed`（DerivedGps），过滤非速度的 Distance 通道，无速度通道时回退第一通道；
    - 在 `src/state/appStore.ts` 的 `openDataset` 接入 `selectDefaultSpeedChannel`；
    - 在 `src/components/PlotStack.tsx` 中移除精准硬编码 `Speed`，使用 `selectDefaultSpeedChannel` 动态解析当前绘图通道；
    - 在 `src/api/workflow.ts` 中同步使用 `selectDefaultSpeedChannel` 请求窗口帧与触发优先读取；
    - 在 `src/vertical-slice.test.ts` 中切换至真实 AGX 官方通道 `GPS Speed (AiM Interpolated)` 并通过全链路断言。
  - 点击优先真实后端实测与证据落盘：
    - 在 `src-tauri/src/state.rs` 和 `main.rs` 统一并暴露 `AppState::prioritize_import`；
    - 将 `ImportJob` 的 `pending` 与 `claimed` 调整为 `pub` 以便精准观测队列状态；
    - 在 `src-tauri/src/imports.rs` 中实现端到端真实测试 `real_xrk_import_prioritize_channel_reorders_queue_and_generates_proof`，接入测试锁隔离进程级 DLL；
    - 使用实际 `Data/AGX.xrk`（626,948 bytes, SHA-256 `af4133b8...`）与官方 AiM DLL 启动后台导入，在 `meta_ready` 后实时读取 pending 队列（共 91 个通道），锁定处于 index 64 的目标通道 `GPS Speed (AiM Interpolated)`；
    - 触发 `prioritize_import`，实时断言目标通道立即重排至 index 0（队列首位）；
    - 等待 worker 认领并由 AiM DLL 读取样本生成金字塔，实时验证 `window_series` 成功返回 960 字节 SXK1 帧，断言 `job.claimed` 包含该目标通道；
    - 触发 `cancel_import` 干净终止剩余导入；
    - 全程实测数据、时间戳、前后队列切片与状态自动落盘至 `.agent/evidence/step5/prioritize-proof.log`。
- 验证：
  - `pnpm test -- --run` 退出码 0（11 个测试套件，78 项测试全部通过）；
  - `pnpm typecheck` 退出码 0；
  - `pnpm lint` 退出码 0（0 错误 0 警告）；
  - `pnpm build` 退出码 0；
  - `cargo fmt --all -- --check` 退出码 0；
  - `cargo clippy --locked --workspace --all-targets -- -D warnings` 退出码 0；
  - `cargo test --locked --workspace` 退出码 0（含 `real_xrk_import_prioritize_channel_reorders_queue_and_generates_proof` 与全量工作区测试）；
  - `powershell -NoProfile -File tests/tooling/check.ps1` 退出码 0（八道门禁全部通过）。
- 证据：
  - `.agent/evidence/step5/prioritize-proof.log`（真实 AGX 导入通道点击优先重排与出图日志）；
  - `src/api/channelSelection.test.ts`、`src/state/appStore.test.ts`、`src/vertical-slice.test.ts`。
- 约束：按指示未提交 Git。

### 2026-09-15 · Step 5 用户实测问题整改与最终回归

- 用户报告：真实 XRK/AGX 导入后总长错误、图表未渲染、界面出现“窗中窗”、部分文字被遮挡。主代理亲自定位和修改；agy-staff 仅执行只读检测，未参与代码写入。
- 根因与修复：
  - 前端错误地按扁平结构读取 `DatasetMeta`，而真实后端返回 `{ id, file_hash, meta, channels }`；现已统一改为读取 `dataset.meta.duration/file_name`，并增加真实 DTO 回归测试。`Data/AGX.xrk` 实机显示总长 `89.00 s`、92 个通道。
  - uPlot 曾把 CSS `var(...)` 直接交给 Canvas，且只绘制 min 包络、复用实例时保留旧通道/单位并禁止 Y 轴重算；现已预解析主题颜色、绘制 min/max 包络带，在通道/单位/主题改变时重建实例，并在新帧到达时自动缩放 Y 轴。
  - 旧的全局 demo CSS 与 AppShell/Dockview 样式叠加，造成双层容器、最小宽度溢出和文字遮挡；现已移除冲突样式、补齐 flex 最小尺寸与层级、调整面板最小宽度和响应式断点，并将 Tauri 默认窗口调整为 `1280x680`（最小 `960x600`、居中）。
  - 修复活跃通道长期锁定默认 Speed、第二次导入被旧 dataset 阻止、图表框选残留、Timeline 拖动高频 IPC、LapsPanel 读取不存在字段，以及 Rust lib/bin 重复执行同一测试的问题。
- 实机回归：
  - `agx-final-window.png`：真实 `Data/AGX.xrk` 导入完成，显示 `GPS Speed (AiM Interpolated)` 曲线、`89.00 s` 总长、92 个通道，界面无窗中窗或文字遮挡；
  - `agx-zoom-result.png`：拖拽缩放后 generation 从 1 增至 2，视口变为 `1.18–30.60 s`，曲线正常重绘且总长保持 `89.00 s`；
  - `prioritize-proof.log`：真实导入过程中将目标速度通道从 pending index 64 提升至 0，随后被 worker 认领并返回 SXK1 帧。
- 只读复审：agy-staff 作业 `review-mu29nyqk-848a0ee5` 找到的实例复用、活跃通道、二次导入、选择框、Timeline IPC、窄屏隐藏与精度问题均由主代理逐项验证并整改；其测试并发造成的 DLL 冲突未当作产品缺陷，结束并发后独立全量验证通过。
- 最终验证：`powershell -NoProfile -File tests/tooling/check.ps1` 退出码 0；依赖契约、guard、rustfmt、clippy、Rust 全量测试、TypeScript、lint、12 个 Vitest 文件共 89 项测试、前端 build、golden suite 全部通过（`All six gates passed.`）。
- 证据：`.agent/evidence/step5/final-corrective-validation-20260915.md`、`.agent/evidence/step5/agx-final-window.png`、`.agent/evidence/step5/agx-zoom-result.png`、`.agent/evidence/step5/prioritize-proof.log`。
- 约束：未执行 git commit / push。


### 2026-09-15 · UI 设计定稿（D16 · F1 官方风格）与文档修订

- 目标：将负责人已审核的 `design-demos/08-f1-data-analysis.html` 定稿为正式软件设计基准，并让设计文档（施工手册附录 B）遵循该 design 文档。
- 实际工作（本轮仅文档，无源码改动）：
  - `design-demos/DESIGN-SPEC.md` 新增第 9 节"正式软件落地决策"：浅色主题保留（红+白 token 推导表）、三栏宽度可拖调整（左 220–420/右 240–480）、状态栏保留、dockview 移除、圈速面板不做、弯道编号第一版不绘制、SIZE chip 需后端补字段；
  - 施工手册 0.1 新增 **D16** 冻结决策，C3 加 dockview 修订标注；附录 B 整体重写为 **rev.5**（B.0 F1 设计原则 / B.1 双主题令牌+红色边界清单+通道固定色板+字体版权边界 / B.2 三栏可拖调宽布局 / B.3 F1 组件规范 / B.4 P1–P9 面板重写（P4 圈速取消）/ B.5 图表渲染参数对齐 demo / B.6 主题实现 / B.7 进度条红填充 / B.9 播放快捷键与 CursorValues 节流 / B.10 走查单加红色边界合规 / **B.11 交互核心约定**（游标按住拖动、禁 hover 跟随、焦点缩放 ×1.18、时间轴窗口跟手、播放 0.05s/帧））；
  - 新增 **Step 5R**（F1 视觉定稿重构，R1 令牌 → R2 壳层 → R3 中栏 → R4 右栏 → R5 播放），插入于 Step 5 与 Step 6 之间；
  - Step 7/8/10/11/15 的 UI 规格引用逐条修订为 rev.5（框选缩放与 hover 十字线废止、通道行 3px 红边条、StatsPanel 改通道详情卡全程口径、赛道图白管+红箭头+HUD、对比模式废止 +4 偏移轮转）；
  - 新增 **ADR-0008**（UI 视觉定稿为 F1 官方风格，含 Formula1 Display 字体版权边界：仅限车队内部工具，发布版须替换授权字体）并更新 ADR 索引。
- 验证：文档静态校对——手册标题结构完整（Step 1–5R–6–15、附录 B.0–B.11 齐全）；未运行测试（本轮无代码变更，不冒充门禁结果）。
- 证据：`design-demos/DESIGN-SPEC.md`（第 9 节）、施工手册（D16、Step 5R、附录 B rev.5）、`.agent/adr/0008-f1-visual-design.md`。
- 未完成 / 下一步：按 Step 5R 实施前端重构（先 R1 令牌层：字体资产 + tokens.css + channelColors.ts）；未执行 git commit / push。

### 2026-09-15 · Step 5R R1 令牌层落地（F1 字体/令牌/通道色板）

- 目标：实施 Step 5R 第 1 阶段（R1 令牌层），不动壳层结构（R2 起）。
- 实际工作：
  - 字体资产：拷贝 `design-demos/fonts/` 五个文件至 `src/assets/fonts/`（Formula1-Display Regular/Bold woff2 + Titillium Regular/SemiBold/Bold ttf）；新增 `src/theme/fonts.css` @font-face（F1 Display 400/700、Titillium 400/600/700），`src/main.tsx` 引入；
  - `src/theme/tokens.css` 按 B.1 rev.5 重写双主题：F1 基础 token（--bg/--bg2/--panel/--line/--red/--text/--dim/--dim2）+ 语义色（--purple/--green/--orange）+ Shell 尺寸（顶栏 54px/状态栏 24px/时间轴 110px/左右栏默认宽）；保留 rev.4 旧 token 兼容别名段（标注过渡期禁新增消费）与 dockview 过渡别名（R2 删除）；浅色 = 红+白按 DESIGN-SPEC 9.1（语义色加深：--purple #8F3BD9、--green #2E8B1F）；
  - `src/index.css`：根字体切换为 Titillium，新增 `.f1`（F1 Display 工具类）与 `.tnum`（tabular-nums）；
  - 新增 `src/theme/channelColors.ts`：通道固定色板匹配（SPEED→var(--text) 前景色派生、RPM/THROTTLE/BRAKE/STEERING/GEAR/LAT G 六色、Distance 显式排除、大小写不敏感）+ 保留池（#3A9BFF/#FF5D8F/#00D9B0）按数据集通道全序确定性分配；
  - 新增 `src/theme/channelColors.test.ts`：8 项测试（SPEED 家族、Distance 排除、六色匹配、Latitude 无误匹配、池分配确定性、池耗尽循环、重复名去重）。
- 验证：`pnpm typecheck` 退出码 0；`pnpm lint` 退出码 0（0 警告）；`pnpm test` 退出码 0（13 个测试套件 97 项全过，含新增 8 项）；`pnpm build` 退出码 0（字体资产正常打包：2 woff2 + 3 ttf）。
- 未完成 / 下一步：Step 5R R2 壳层重构（固定三栏 Grid + 拖宽分割条 + 54px 顶栏 + 移除 dockview）；未执行 git commit / push。

### 2026-09-21 · 01 · 工作区被旧快照覆盖的事故与恢复

- 现象：负责人报告「软件被 GPT 清空」。核实：工作区在今天 00:55–01:05 由外部生成了一份旧版快照，并在上午 11:03–11:15 以**保留时间戳**的方式多次覆盖本目录（证据：design-demos/DESIGN-SPEC.md 20 分钟内体积由 15490→23527 而 mtime 不变；本次会话中写入的日志追加曾凭空消失）。
- 受损范围：F1 设计之后的成果从磁盘消失。tokens.css 无 --red、AppShell 无 logo_white、LibraryView 32KB→2.9KB（分类/分组/明细/二级栏全丢）、TrackMapPanel 回退成「Step 11 功能预留」占位、PlotStack/uPlotFactory 无 spanGaps/yRangeFromData、TimelineBar 无 ×3 灵敏度、ChannelTree 无选中置顶；另有 15 个文件被删（export.rs、stats_cmd.rs、window_fit.rs、capabilities/default.json、hooks/useCursorValues.ts、utils/viewport.ts 及 3 个组件测试、3 个 utils 测试、assets.d.ts、start/build/dev.cmd）；.agent/adr/0009~0011 被删、本日志被截断至 09-15 R1。
- 恢复源（回收站空、git 无这些未跟踪文件的 blob）：① ZCode 文件快照库 ~/.zcode/cli/artifacts/（每次写入的 afterContent，上限约 09-15/16）；② ZCode 数据库 db.sqlite 工具调用全历史（上限同）；③ 本次会话已读取的窗口文件。**09-17/18 的精修（原生拖放、走查九项、红栏 logo、资料库二级栏末端形态）不在任何自动恢复源中。**
- 已执行恢复（负责人拍板「按 09-16 基线恢复」，无自有备份）：附加性恢复 15 个被删文件；按快照恢复 tokens.css / LibraryView / FileCard / PlotStack / ColumnSplitter / TrackMapPanel / StatsPanel / channelColors / time.ts / viewport 等；AppShell 保留备份版（比快照版多库页与 viewOverride）。补齐胶水：client.ts 新增 pickExportFolder/exportRecords/ExportOutcome；uPlotFactory 新增 createStackedOptions；FileCard 抽出 FileCardView + pickBestLap；src-tauri/Cargo.toml 补回 [features] custom-protocol；logo 从 aim-telemetry-overlay 取同款占位（负责人另补）。
- 验证：tsc 0 错误；vitest 18 套件 135 项全过；eslint 0 错误；vite build 成功；cargo check 通过；cargo build --release --features custom-protocol 完成（exe 10.1MB，较前版 +202KB 即内嵌资源）。
- 现状与差距：恢复后为 09-16 基线形态——F1 令牌/图表/赛道图/资料库页在，但**红色一级栏 + logo 头部、原生拖放、走查九项、导出四件套接线、窗口自适应（ADR-0011）、CSV 变体与去重**仍需按 DESIGN-SPEC（完好）与施工手册（完好）重做。
- 恢复产物已外移仓库防再次被覆盖：`D:\Desktop\SCUTRacingTelemetry-recovery\`（restored/ snapshots/ recovered2/ backup-pre-restore/ 与恢复脚本）。
- **未执行 git commit / push**——强烈建议立刻提交一个基线，否则再次覆盖会全部丢失（当前所有成果仍在工作区）。

### 2026-09-21 · 02 · 按聊天记录还原前端（走查九项 + 红栏 + 拖放 + R5）

- 依据：从会话数据库提取的 76 条 UI 需求原文（含 walkthrough「吐槽点有点多…」全文与九项清单）。
- logo：按负责人指定，用 `tests/logo.png`（586×74）替换 `src/assets/logo_white.png`（design-demos/、Data/ 同步）。
- 逐项落地：
  1. 图表颜色不用白色——`SPEED_COLOR` 由 `var(--text)` 改为 `#00E5FF`，未匹配保留池 3→6 色（注释同步「不给出白色曲线」）。
  2. 赛道图——白管双层（7px+4px）改单线 1.5px；尾迹 2.5→1.5px；箭头缩约 40%（9/-6/-3 → 5.5/-3.5/-1.8）；起终点 3×16→2×10；HUD「T·CROSSTIME」→「TIME」。
  3. 图表——`createStackedOptions` 重写为单线（删 max 系列与包络带）、`spanGaps: true`、数据点关闭、图例关闭、`scales.y.auto=false + range=yRangeFromData`（有限值 +10% pad、常数 ±0.5、空 0..1）；新增 `frameToSingleLine`（包络取中线）；PlotStack 传 `xAxisVisible = isLast`（x 轴刻度只在最底图）；index.css 游标改 2px 实线并隐藏横向线。
  4. 一级栏——红底（--red）+ 白色 logo 图 + 英文 tab（DATABASE / TELEMETRY VIDEO / WIFI DOWNLOAD，白字白下划线，3px 激活下划线）；分析页删文件名只留「DATA ANALYSIS」；**删除路径导入输入**（导入只在库页二级栏 + 拖入）；按钮白描边白字（GhostButton onRed）；主题按钮改「☾ Dark / ☀ Light」；右上状态灯删除。
  5. 时间轴——拖动灵敏度 ×3（`TIMELINE_PAN_SENSITIVITY`，用户反馈幅度太小），测试期望同步。
  6. 通道列表——已勾选通道独立成 `channel-pinned` 置顶区（flex:none，不随下方列表滚动）；全局 6px 细滑条（灰色圆角 thumb）。
  7. FileCard 瘦身——文件名 15→13px；删「通道 Channels」「最快圈 Best Lap」两行；DATA FILE 标题 padding 去双缩进（12px 14px 8px → 12px 0 8px）与 CHANNELS 左缘对齐；控件文案英文化（Reset zoom / Prioritize this channel / Search channels…）。
  8. R5 播放——appStore 补 `playing`/`setPlaying`（切视图自动暂停）；AppShell 加真实帧间隔推进游标（到窗口末端回到起点）与空格键播放/暂停；分析页 EXPORT 接 `pickExportFile` + `exportCsv`。
  9. 原生拖放——库页原用 HTML5 onDragOver/onDrop（WebView2 会拦截，拖入无效），改为 `getCurrentWebview().onDragDropEvent`（enter/over 灰化遮罩、drop 导入、leave 撤销）。
- 验证：tsc 0 错误；eslint 0 错误；vitest 18 套件 **135 项全过**（同步了 App/FileCard/TimelineBar/channelColors 的旧断言）；vite build 成功；cargo build --release --features custom-protocol 完成（exe 10.1MB）。
- 仍未还原（需继续，均不在自动恢复源内）：时间轴 canvas 化（现为 36px DOM 滑条，目标 110px 画布 + 红窗口框 + 缩略曲线）；导出四件套后端接线（export.rs/stats_cmd.rs 已恢复但未挂进 lib.rs/main.rs）；窗口自适应（ADR-0011）；CSV 变体 + 重复时间戳去重（csv-parser 侧）；`crates/csv-parser/tests`。
- **未执行 git commit / push**（再次提醒：成果仍只在工作区）。

### 2026-09-21 · 03 · logo 更换 + 未还原项全部补齐 + 首次提交推送

- logo：负责人指定 `tests/logo_white.png`（1161×143 RGBA 带透明通道）替换 `src/assets/logo_white.png`（design-demos/、Data/ 同步）。上一版用的 `tests/logo.png` 是无 alpha 的灰度图，在红栏上会显示成黑底方块。
- 补齐上轮遗留的四项：
  1. **导出四件套接线**：`export_records` / `export_channels` / `pick_export_folder` / `pick_export_file` 四个命令在 main.rs 注册；`telemetry-ipc` 补回被回退掉的 `ExportOutcome` 与 `ChannelStatsDto`；`stats` 命令由桩改为接 `stats_cmd::window_stats`（按 dataset id 取 hash → 读库内 source.csv 全分辨率统计）。export.rs 的 missing 分支补了 `write_manifest_only` fixture（旧版记录：有 manifest 无 source.csv），3 项导出测试全过。
  2. **窗口自适应（ADR-0011）**：`tauri-plugin-window-state` 三处登记（根/Cargo、src-tauri/Cargo、依赖契约）→ 启动 `.setup(clamp_main_window)` 用 `window_fit::fit_into_area` 把窗口夹进屏幕可用区（最大化/全屏时跳过）。
  3. **CSV 变体与去重**：`csv_io::read_aim_csv` 已有 BOM 剥离与按内容识别分块，本轮补「非严格递增时间戳排序去重（同刻保留最后一个样本）」并加 2 项单测（排序去重、BOM）。
  4. **时间轴 canvas 化**：36px DOM 滑条 → 110px 画布（红窗口框 + 速度包络缩略曲线 + 白色 2px 游标），拖动/点击手势保留并接到画布容器，灵敏度 ×3 不变。
- 顺带修正：Tauri 原生拖放订阅移入 `api/client.onFileDrop`（此前在 LibraryView 直接 import `@tauri-apps/api/webview`，违反 IPC 边界契约，守卫已报错）；`cargo fmt` 重排后暴露的 4 处 clippy（doc 缩进、`map_err`→`inspect_err`、`sort_by_key`）一并修掉。
- 验证：`cargo clippy --workspace --all-targets -D warnings` 干净；workspace 测试全过（src-tauri 串行 19/19；并行的 xrk 用例为既有 DLL 争抢，单跑必过）；tsc 0 错误；eslint 0 错误；vitest 18 套件 **135 项全过**；`verify_dependencies.py` PASS；release 重建 10.4MB（`--features custom-protocol`）。
- **git：首次提交并推送成功** —— `d2f076c`（74 个文件）→ `origin/main`，此前最近提交是 09-15。推送需经本地代理 `127.0.0.1:7897`（直连 GitHub 被重置）。
- 仍未做（非本轮范围）：时间轴缩略曲线目前取默认速度通道包络，未做低速通道回退；库页「WiFi 下载 / TELEMETRY VIDEO」仍为占位 tab。
